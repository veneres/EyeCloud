import { AttentionCloudDirective } from './attention-cloud.directive';

describe('AttentionCloudDirective', () => {
  it('should create an instance', () => {
    const directive = new AttentionCloudDirective();
    expect(directive).toBeTruthy();
  });
});

import { GazeStripesDirective } from './gaze-stripes.directive';

describe('GazeStripesDirective', () => {
  it('should create an instance', () => {
    const directive = new GazeStripesDirective();
    expect(directive).toBeTruthy();
  });
});

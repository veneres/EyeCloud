import { RandomPosDirective } from './random-pos.directive';

describe('RandomPosDirective', () => {
  it('should create an instance', () => {
    const directive = new RandomPosDirective();
    expect(directive).toBeTruthy();
  });
});
